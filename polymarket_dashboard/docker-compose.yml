services:
  # questdb:
  #   image: questdb/questdb:latest
  #   container_name: questdb
  #   ports:
  #     - "9000:9000"   # Web console
  #     - "8812:8812"   # SQL over TCP
  #   volumes:
  #     - questdb-data:/root/.questdb

  api:
    build:
      context: .
      dockerfile: deployment/Dockerfile
    container_name: "api"
    ports:
      - "8000:8000"
    volumes:
      # - ./src/polymarket_dashboard/src/api/:/app
      - ./src:/app
      - ./.env:/app/.env
      - ./pyproject.toml:/app/pyproject.toml
    # depends_on:
    #   - questdb


# volumes:
  # questdb-data:
